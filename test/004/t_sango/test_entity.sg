import "sango.debug" -> debug ;;
import "sango.io.stdio" -> stdio ;;
import "sango.entity.feature" -> entf ;;
import "sango.entity.box" -> box ;;
import "sango.unique.feature" -> uniqf ;;

eval _main_ -> <void> {
  test
} ;;

eval test -> <void> {
  test000,
  void$
} ;;

eval test000 -> <void> {
  3 none$ box.create_box = *B,
  B entf.incarnate = *BI,
  BI &(BI uniqf._feature_a'unique) >> debug.repr >> stdio.print_and_newline
} ;;
