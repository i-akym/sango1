import "sango.debug" -> debug ;;
import "sango.io.stdio" -> stdio ;;
import "sango.util.col" -> col ;;

eval _main_ -> <void> {
  test
} ;;

eval test -> <void> {
  test000,
  void$
} ;;
eval test000 -> <void> {
  [ 1, 2, 3 ] list_col_fs col.combine = *C,
  C col.empty? = false$,
  C col.size = 3,
  C col.iter = *I,
  I next = (| 1, *I1 |) value$,
  I1 next = (| 2, *I2 |) value$,
  I2 next = (| 3, *I3 |) value$,
  I3 next = none$,
  void$
} ;;
