import "sango.debug" -> debug ;;
import "sango.io.stdio" -> stdio ;;
import "sango.util.eq.feature" -> eqf ;;
import "sango.util.ord.feature" -> ordf ;;

eval _main_ -> <void> {
  test
} ;;

eval test -> <void> {
  test000,
  test100,
  void$
} ;;
eval test000 -> <void> {
  3 ordf.ord_inc$ = *O3,
  5 ordf.ord_inc$ = *O5,
  O3 O5 eqf.eq? = false$,
  O3 O5 eqf.ne? = true$,
  O3 eqf.hash >> debug.repr >> stdio.print_and_newline
} ;;
eval test100 -> <void> {
  (3 ordf.ord_inc$) eqf.eq_inc$ = *E3,
  E3 eqf.fx >> ordf.fx = 3,
  void$
} ;;
