import "sango.debug" -> debug ;;
import "sango.io.stdio" -> stdio ;;
import "sango.util.stack" -> stack ;;
import "sango.util.stack.liststack" -> lstack ;;

eval _main_ -> <void> {
  test
} ;;

eval test -> <void> {
  test000,
  void$
} ;;
eval test000 -> <void> {
  lstack.new_stack [ 1, 2, 3 ] stack.push_list = *S3,
  S3 stack._feature_a'stack = *SF,
  SF stack.stack_fs_to_col_fs = *CF,
  S3 &(CF empty?_f) = false$,
  S3 &(CF size_f) = 3,
  S3 &(CF iter_f) = *I3,
  I3 iter_next = (| **, *I2 |) value$,
  I2 iter_next = (| **, *I1 |) value$,
  I1 iter_next = (| **, *I0 |) value$,
  I0 iter_next = none$,
  void$
} ;;
